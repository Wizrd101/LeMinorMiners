using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering.Universal;

//public class LightFlicker : MonoBehaviour
//{
  //  float intensity;
   // public float intensityMax;
   // public float intensityMin;
   // public float randomMin = 0f;
   // public float randomMax = 10f;


   // Light2D light1;

    // Start is called before the first frame update
  //  void Start()
  //  {
  //      Invoke("RandomFlicker", Random.Range(randomMin, randomMax));
  //      light1 = GetComponent<Light2D>();
  //      intensity = light1.intensity;
  //      intensityMax = (float)(intensity + 0.5);
   //     intensityMin = (float)(intensity - 0.25);

  //  }

    // Update is called once per frame
   // void Update()
   // {
   //     intensity = Random.Range(intensityMin, intensityMax);
  //      light1.intensity = intensity;

   // }

  //  void RandomFlicker()
  //  {
  //      float randomTime = Random.Range(randomMin, randomMax);

   //     Invoke("RandomFlicker", randomTime);
   // }
//}
